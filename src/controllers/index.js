const {
  getCartController,
  creatCartController,
  updateCartController,
  removeCarcontroller,
} = require('./cartControllers');

module.exports = {
  cartController: {
    getCartController,
    creatCartController,
    updateCartController,
    removeCarcontroller,
  },
};
