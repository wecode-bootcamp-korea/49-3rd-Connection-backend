const {
  getCartDao,
  easyCheckDao,
  creatCartDao,
  // newProductDao,
  // updateQuantityDao,
  // deletCartDao,
} = require('./cartDao');

module.exports = {
  cartDao: {
    getCartDao,
    easyCheckDao,
    creatCartDao,
    // newProductDao,
    // updateQuantityDao,
    // deletCartDao,
  },
};
